"use strict";

var _ilib = _interopRequireDefault(require("ilib"));

require("@testing-library/jest-dom");

var _react = require("@testing-library/react");

var _userEvent = _interopRequireDefault(require("@testing-library/user-event"));

var _DatePicker = _interopRequireWildcard(require("../DatePicker"));

var _jsxRuntime = require("react/jsx-runtime");

function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }

function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { "default": obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj["default"] = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// Note: Tests pass 'locale' because there's no I18nDecorator to provide a value via context and
// otherwise, nothing renders in the label.
describe('DatePicker', function () {
  test('should emit an onChange event with type when changing a component picker', function () {
    var handleChange = jest.fn();
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      onChange: handleChange,
      value: new Date(2000, 6, 15),
      locale: "en-US"
    }));

    var monthPickerUp = _react.screen.getAllByText('▲')[0];

    _userEvent["default"].click(monthPickerUp);

    var expected = 1;
    var expectedType = {
      type: 'onChange'
    };
    var actual = handleChange.mock.calls.length && handleChange.mock.calls[0][0];
    expect(handleChange).toBeCalledTimes(expected);
    expect(actual).toMatchObject(expectedType);
  });
  test('should fire onComplete event with type when enter key pressed from the last picker', function () {
    var handleComplete = jest.fn();
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      onComplete: handleComplete,
      value: new Date(2000, 6, 15),
      locale: "en-US"
    }));

    var year = _react.screen.getByLabelText('2000 year change a value with up down button');

    (0, _react.act)(function () {
      return year.focus();
    });

    _react.fireEvent.keyDown(year, {
      which: 13,
      keyCode: 13,
      code: 13
    });

    var expected = {
      type: 'onComplete'
    };
    var actual = handleComplete.mock.calls.length && handleComplete.mock.calls[0][0];
    expect(actual).toMatchObject(expected);
  });
  test('should fire onSpotlightLeft event with type when spotlight is leaving via left key', function () {
    var handleSpotlight = jest.fn();
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      onSpotlightLeft: handleSpotlight,
      value: new Date(2000, 6, 15),
      locale: "en-US"
    }));

    var month = _react.screen.getByLabelText('7 month change a value with up down button');

    (0, _react.act)(function () {
      return month.focus();
    });

    _react.fireEvent.keyDown(month, {
      which: 37,
      keyCode: 37,
      code: 37
    });

    var expected = {
      type: 'onSpotlightLeft'
    };
    var actual = handleSpotlight.mock.calls.length && handleSpotlight.mock.calls[0][0];
    expect(actual).toMatchObject(expected);
  });
  test('should fire onSpotlightRight event with type when spotlight is leaving via right key', function () {
    var handleSpotlight = jest.fn();
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      onSpotlightRight: handleSpotlight,
      value: new Date(2000, 6, 15),
      locale: "en-US"
    }));

    var year = _react.screen.getByLabelText('2000 year change a value with up down button');

    (0, _react.act)(function () {
      return year.focus();
    });

    _react.fireEvent.keyDown(year, {
      which: 39,
      keyCode: 39,
      code: 39
    });

    var expected = {
      type: 'onSpotlightRight'
    };
    var actual = handleSpotlight.mock.calls.length && handleSpotlight.mock.calls[0][0];
    expect(actual).toMatchObject(expected);
  });
  test('should accept a JavaScript Date for its value prop', function () {
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: new Date(2000, 0, 1),
      locale: "en-US"
    }));

    var year = _react.screen.getByText('2000');

    expect(year).toBeInTheDocument();
  });
  test('should set "dayAriaLabel" to day picker', function () {
    var label = 'custom day aria-label';
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      dayAriaLabel: label,
      value: new Date(2000, 0, 1)
    }));

    var dayPicker = _react.screen.getByLabelText(label);

    var expected = 'day';
    expect(dayPicker).toBeInTheDocument();
    expect(dayPicker).toHaveClass(expected);
  });
  test('should set "monthAriaLabel" to month picker', function () {
    var label = 'custom month aria-label';
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      monthAriaLabel: label,
      value: new Date(2000, 0, 1)
    }));

    var monthPicker = _react.screen.getByLabelText(label);

    var expected = 'month';
    expect(monthPicker).toBeInTheDocument();
    expect(monthPicker).toHaveClass(expected);
  });
  test('should set "yearAriaLabel" to year picker', function () {
    var label = 'custom year aria-label';
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: new Date(2000, 0, 1),
      yearAriaLabel: label
    }));

    var yearPicker = _react.screen.getByLabelText(label);

    var expected = 'year';
    expect(yearPicker).toBeInTheDocument();
    expect(yearPicker).toHaveClass(expected);
  });
  test('should set "data-webos-voice-disabled" to day picker when voice control is disabled', function () {
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: new Date(2000, 0, 1),
      "data-webos-voice-disabled": true
    }));

    var dayPicker = _react.screen.getByLabelText('1 day change a value with up down button');

    var expected = 'data-webos-voice-disabled';
    expect(dayPicker).toHaveAttribute(expected);
  });
  test('should set "data-webos-voice-disabled" to month picker when voice control is disabled', function () {
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: new Date(2000, 0, 1),
      "data-webos-voice-disabled": true
    }));

    var monthPicker = _react.screen.getByLabelText('1 month change a value with up down button');

    var expected = 'data-webos-voice-disabled';
    expect(monthPicker).toHaveAttribute(expected);
  });
  test('should set "data-webos-voice-disabled" to year picker when voice control is disabled', function () {
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: new Date(2000, 0, 1),
      "data-webos-voice-disabled": true
    }));

    var yearPicker = _react.screen.getByLabelText('1900 year change a value with up down button'); // The year is 1900 because it does not change based on props, this needs to be fixed in datePicker


    var expected = 'data-webos-voice-disabled';
    expect(yearPicker).toHaveAttribute(expected);
  });
  test('should format a date the same as the label', function () {
    var date = new Date(2000, 0, 1);
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: date,
      locale: "en-US"
    }));

    var header = _react.screen.getByText((0, _DatePicker.dateToLocaleString)(date)).parentElement.parentElement;

    var expected = 'heading';
    expect(header).toHaveClass(expected);
  });
  test('should format a date the same as the label in another locale', function () {
    _ilib["default"].setLocale('ar-SA');

    var date = new Date(2000, 0, 1);
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: date,
      locale: "ar-SA"
    }));

    var header = _react.screen.getByText((0, _DatePicker.dateToLocaleString)(date)).parentElement.parentElement;

    var expected = 'heading';
    expect(header).toHaveClass(expected);
  });
  test('should not display Heading', function () {
    var date = new Date(2000, 0, 1);
    (0, _react.render)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_DatePicker["default"], {
      value: date,
      locale: "en-US",
      noLabel: true
    }));

    var header = _react.screen.queryByText((0, _DatePicker.dateToLocaleString)(date));

    expect(header).toBeNull();
  });
});